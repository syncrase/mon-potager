import {ReferenceDirective} from './reference.directive';

describe('ReferenceDirective', () => {
  it('should create an instance', () => {
    const directive = new ReferenceDirective();
    expect(directive).toBeTruthy();
  });
});
